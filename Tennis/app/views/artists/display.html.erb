
<h1><%= @song.name %></h2>
<h2><%= @song.description %></h2>
<h2><%= @song.director %></h2>

<%= link_to 'Edit a song', edit_song_path(@song) %>
<%= button_to 'x',song_path(@song),method: :delete%>
<%= link_to 'All songs', '/'%>

    <% @songs.each do |song| %>
        <p><%= song.title %></p>
    <% end %>

<%= link_to 'add a song', new_song_artist_path(@song) %>
